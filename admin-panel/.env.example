# UniTrack mobil backend'iyle AYNI veritabanı (aynı Postgres instance/DB).
DATABASE_URL=postgresql://user:password@localhost:5432/unitrack

# `openssl rand -base64 32` ile üretin.
BETTER_AUTH_SECRET=

# Admin panelin kendi adresi (prod'da gerçek domain).
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# /legal/delete-account'taki "hesap silme talebi" onay e-postası için.
# Tanımsız bırakırsan (dev'de olduğu gibi) gerçekten göndermez, konsola yazar.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=furkanugurlu6806@gmail.com
SMTP_PASS=abcdxyzqwertyuio
SMTP_FROM=noreply@unitrack.app

